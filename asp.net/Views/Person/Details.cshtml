@model BobKatRegistry.Models.Person

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>Person</legend>

    <div class="display-label">GivenName</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.GivenName)
    </div>

    <div class="display-label">Siurname</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Siurname)
    </div>

    <div class="display-label">Email</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Email)
    </div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.PersonId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
